import 'package:flutter/material.dart';

class CreationTicketPage extends StatelessWidget {
  const CreationTicketPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
